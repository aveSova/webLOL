// config.js (ES modules version)
const config = {
    port: 3000,
    db: {
        host: 'localhost',
        user: 'root',
        password: '13577531admin',  // Ваш пароль MySQL (если есть)
        database: 'form_db',
        port: 3306
    }
};

export default config;